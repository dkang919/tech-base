name: Update BigQuery Data Chart

on:
  schedule:
    - cron: '0 2 * * 0'   # 캐나다 동부(EST) 토요일 저녁 9시 = UTC 일요일 오전 2시
  workflow_dispatch:      # 수동 실행 버튼 (테스트용)

jobs:
  update-chart:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
              run: |
                pip install -r requirements.txt

      - name: Run Analysis
        env:
          # 시크릿에 저장한 키를 임시 파일로 변환하여 인증에 사용
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
        run: |
          echo "$GCP_SA_KEY" > gcp-key.json
          export GOOGLE_APPLICATION_CREDENTIALS=gcp-key.json
          python scripts/generate_charts.py
          rm gcp-key.json # 보안을 위해 파일 삭제

      - name: Commit and Push
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/assets/charts/category_price_trend.html
          git commit -m "📊 Auto-update: Latest BigQuery charts" || echo "No changes to commit"
          git push